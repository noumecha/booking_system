{#
/**
 * @file booking_reservation.html.twig
 * Default theme implementation to present Booking reservation data.
 *
 * This template is used when viewing Booking reservation pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_booking_reservation()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('booking_reservation') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
